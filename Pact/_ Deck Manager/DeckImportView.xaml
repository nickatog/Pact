<Window x:Class="Pact.DeckImportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Pact"
        mc:Ignorable="d"
        Title="DeckImportView" Height="120" Width="300" WindowStyle="None" WindowStartupLocation="CenterOwner">
    <StackPanel>
        <TextBox Text="{Binding DeckString}" MinLines="3" MaxLines="3" TextWrapping="Wrap" AcceptsReturn="True" FontSize="20" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            
            <Button Content="Cancel" Grid.Column="0" IsCancel="True" />
            <Button Content="Import" Grid.Column="1" Command="{Binding ImportDeck}" />
        </Grid>
    </StackPanel>
</Window>
