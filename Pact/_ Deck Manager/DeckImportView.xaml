<Window x:Class="Pact.DeckImportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Pact"
        mc:Ignorable="d"
        Title="Import Deck" Height="200" Width="300" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,2,2,0">
            <Label Content="Title:" />
            <TextBox Text="{Binding DeckTitle}" />
        </DockPanel>

        <TextBox Grid.Row="1" Text="{Binding DeckString}" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="2,2,2,0" />

        <Grid Grid.Row="2" Margin="2,2,2,2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="0" Content="Cancel" IsCancel="True" Margin="0,0,1,0" />
            <Button Grid.Column="1" Content="Import" Command="{Binding ImportDeck}" Margin="1,0,0,0" />
        </Grid>
    </Grid>
</Window>
