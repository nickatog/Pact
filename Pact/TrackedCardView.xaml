<UserControl x:Class="Pact.TrackedCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pact"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>

        <Label Content="{Binding Cost}" Grid.Column="0" HorizontalAlignment="Right" />

        <Label Content="{Binding Name}" Grid.Column="1" HorizontalAlignment="Left" />

        <Label Content="{Binding Count}" Grid.Column="2" HorizontalAlignment="Right" />
    </Grid>
</UserControl>
